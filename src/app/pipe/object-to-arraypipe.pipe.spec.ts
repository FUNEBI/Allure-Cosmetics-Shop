import { ObjectToArraypipePipe } from './object-to-arraypipe.pipe';

describe('ObjectToArraypipePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectToArraypipePipe();
    expect(pipe).toBeTruthy();
  });
});
